let browser = ['Chrome','firefox','safari','edge'];

//console.log(browser);

for(let i=0; i<browser.length; i++)
{
    console.log(browser[i]);
}

for(let browsers of browser)
{
    console.log(browsers)
}